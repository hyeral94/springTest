<?xml version="1.0" encoding="UTF-8"?>

<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
 "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
 
 <mapper namespace="com.som.test.lesson06.dao.WeatherhistoryDAO">
 
 	<select id="selectWeatherhistoryList" resultType="com.som.test.lesson06.model.Weatherhistory">
 		SELECT 
 			`id`
 			, `date`
 			, `weather`
 			, `temperatures`
 			, `precipitation`
 			, `microDust`
 			, `windSpeed`
 			, `createdAt`
 			, `updatedAt`
 			
 		FROM `weatherhistory`
 	
 	</select>
 	
 	
 	<insert id="insertWeatherhistoryList" parameterType="map">
 		INSERT INTO `weatherhistory`
	 		(
		 		`date`
	 			, `weather`
	 			, `temperatures`
	 			, `precipitation`
	 			, `microDust`
	 			, `windSpeed`
	 			, `createdAt`
	 			, `updatedAt`
		 		
	 		)
	 	VALUES	
	 		(
	 		<!-- DAO에 @param 이름과 같은 값 -->
	 			#{date},
				#{weather},
				#{temperatures},
				#{precipitation},
				#{microDust},
				#{windSpeed},
				now(),
				now()
	 		
	 		)
 	
 	</insert>
 </mapper>